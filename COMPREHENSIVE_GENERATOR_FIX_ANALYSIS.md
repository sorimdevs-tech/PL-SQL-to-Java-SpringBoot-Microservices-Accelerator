"""
COMPREHENSIVE GENERATOR ANALYSIS & IMPLEMENTATION ROADMAP
Complete PL/SQL-to-Java Conversion Fix Plan (6 Critical Changes)

Date: April 6, 2026
Status: READY TO IMPLEMENT
Priority: CRITICAL - These fixes are required for logically correct Java generation
"""

# ============================================================================
# EXECUTIVE SUMMARY
# ============================================================================

EXECUTIVE_SUMMARY = """
╔═══════════════════════════════════════════════════════════════════════════╗
║                      GENERATOR PROBLEM STATEMENT                         ║
╚═══════════════════════════════════════════════════════════════════════════╝

THE CORE ISSUE:
───────────────
The PL/SQL-to-Java generator produces Java code with Oracle PL/SQL syntax
mixed in, invalid JPA calls, and incorrect return types. These are not LLM
hallucinations—they're generated by deterministic Python template functions
that build Java code by concatenating strings from metadata, with NO AI
validation applied.

PROOF THE LLM ISN'T CALLED:
──────────────────────────
1. generate_services_from_semantics() routes everything to Python templates
2. The LLM's 12 strict rules exist in PromptTemplate but are never used
3. Oracle syntax (NVL, SUBSTR, INSTR, apex_web_service) appears in output
4. Invalid JPA patterns (findOne(null), named parameters) appear in output
5. Validation feedback loop exists but isn't wired into generation

ROOT CAUSE:
──────────
generate_services_from_semantics() has TWO paths:
  • _generate_utility_service_from_unit() - for no-SQL packages
  • _generate_deterministic_service_from_unit() - for data packages

  The second path builds Java by string concatenation with NO LLM check.
  Even when there are validation_feedback violations (detected by validator),
  they're never passed back to generation for correction.

THE 12 MISSING RULES (From PromptTemplate, Never Enforced):
────────────────────────────────────────────────────────────
1. ✗ No Oracle SQL functions (NVL, INSTR, SUBSTR, TO_CHAR)
2. ✗ No PL/SQL naming (p_*, l_* should be camelCase)
3. ✗ No named parameter syntax (key = value)
4. ✗ No single quotes (should be double quotes)
5. ✗ No := operator (should be =)
6. ✗ No apex_web_service (use RestTemplate)
7. ✗ No CASE/WHEN (use Switch or ternary)
8. ✗ No package-qualified refs (_pkg.function)
9. ✗ Proper JPA calls (findById not findOne)
10. ✗ Return specific types, not Object
11. ✗ Include all validation steps
12. ✗ @Transactional annotations for autonomy

CURRENT STATE:
──────────────
✗ Validation catches violations but output is still broken
✗ Corrector has regex patterns to fix code but corrector isn't called
✗ LLM never sees complex packages
✗ Simple CRUD packages get deterministic (wrong) code
✗ User has to manually scrub generated Java


HOW THIS WILL BE FIXED:
───────────────────────
Fix #1: Wire the corrector into the pipeline (1 line)
        → service_code_corrector.correct_plsql_syntax() called on all output

Fix #2: Route complex packages to LLM (change 1 conditional)
        → generate_services_from_semantics() checks if package is complex
        → If complex, uses LLM with PromptTemplate instead of Python

Fix #3: Remove the useless for(rowIndex=0; rowIndex<1) loop
        → Only needed for pagination; for single-record CRUD, remove it

Fix #4: Fix _resolve_lookup_argument() to never emit named params
        → Return only the argument value, not "key = value"

Fix #5: Expand _validate_java_code() to catch all Oracle syntax
        → Catches instr, substr, nvl on deterministic+LLM output alike

Fix #6: Move inner BusinessException class outside method body
        → Fix the syntactically invalid inner class placement

EXPECTED OUTCOME:
─────────────────
✓ All generated Java code valid Java (compiles without syntax errors)
✓ No Oracle syntax mixed into Java
✓ Proper JPA patterns (findById().orElse() not findOne(null))
✓ Proper variable naming (camelCase not p_paramName)
✓ Proper type system (specific types not Object)
✓ All PL/SQL logic preserved in Java (validated by control-flow extraction)
✓ @Transactional annotations where needed
✓ All 12 rules enforced on 100% of output

TIME ESTIMATE:
──────────────
Fix #1 (corrector): 15 min - 1 simple integration
Fix #2 (LLM routing): 30 min - Change conditional + add route
Fix #3 (loop removal): 20 min - Remove unnecessary code
Fix #4 (arg resolution): 20 min - Fix _resolve_lookup_argument()
Fix #5 (validation): 30 min - Expand regex patterns
Fix #6 (class placement): 10 min - Move code in generated string

TOTAL: ~2 hours implementation + testing


═════════════════════════════════════════════════════════════════════════════
"""

# ============================================================================
# DETAILED ANALYSIS OF EACH FIX
# ============================================================================

FIX_1_CORRECTOR_INTEGRATION = """
FIX #1: WIRE SERVICE CORRECTOR INTO GENERATION PIPELINE
════════════════════════════════════════════════════════

PROBLEM:
────────
service_code_corrector.py has working regex patterns:
  ✓ correct_plsql_syntax() removes :=, converts 'strings' to "strings"
  ✓ Regex for Oracle functions (instr, substr, nvl)
  ✓ Regex for named parameter syntax
  ✓ Regex for package-qualified refs

But correct_plsql_syntax() is NEVER CALLED on generated output.

EVIDENCE:
─────────
Current flow:
  generate_services_from_semantics()
    └─ _generate_deterministic_service_from_unit()
       └─ Return raw Java strings
  
  → (Java written to files with Oracle syntax still in it)

Should be:
  generate_services_from_semantics()
    └─ _generate_deterministic_service_from_unit()
       └─ Apply service_code_corrector.correct_plsql_syntax()
       └─ Return cleaned Java strings
  
  → (Java written to files, clean and valid)

IMPLEMENTATION:
────────────────

Location: src/converter/llm_engine.py
Function: generate_services_from_semantics()

Current code (around line 1580):
────────────────────────────────
    for filename in service_order:
        unit = selected_units[filename]
        unit_name = unit.get('name', '')
        service_feedback = feedback_for_service.get(filename, []) or feedback_for_service.get(unit_name, [])
        
        if self._is_utility_unit(unit):
            services[filename] = self._generate_utility_service_from_unit(
                unit=unit,
                all_units=source_units,
            )
        else:
            services[filename] = self._generate_deterministic_service_from_unit(
                unit=unit,
                entities=entities,
                repositories=repositories,
                all_units=source_units,
                metadata_provider=metadata_provider,
                validation_feedback=service_feedback,
            )
    return services

CHANGE TO:
──────────
    from ..validator.service_code_corrector import ServiceCodeCorrector  # Add at top
    
    for filename in service_order:
        unit = selected_units[filename]
        unit_name = unit.get('name', '')
        service_feedback = feedback_for_service.get(filename, []) or feedback_for_service.get(unit_name, [])
        
        if self._is_utility_unit(unit):
            services[filename] = self._generate_utility_service_from_unit(
                unit=unit,
                all_units=source_units,
            )
        else:
            services[filename] = self._generate_deterministic_service_from_unit(
                unit=unit,
                entities=entities,
                repositories=repositories,
                all_units=source_units,
                metadata_provider=metadata_provider,
                validation_feedback=service_feedback,
            )
        
        # FIX #1: Apply corrections to remove PL/SQL syntax
        services[filename] = ServiceCodeCorrector.correct_plsql_syntax(services[filename])

    return services

IMPACT:
───────
✓ All generated Java code cleaned of:
  • := operators (converted to =)
  • Single quotes (converted to double)
  • Named parameter syntax (removed)
  • Package-qualified references (_pkg.foo removed)
  • Obvious Oracle SQL functions in string form

✓ Works on BOTH utility services AND deterministic services
✓ Cumulative with other fixes

EXAMPLES:
─────────
Before:
  p_customer_id := getCustomerId()
  String result = SUBSTR(name, 1, 10)
  String sql = 'SELECT * FROM CUSTOMERS'
  
After:
  pCustomerId = getCustomerId()
  String result = name.substring(0, Math.min(10, name.length()))
  String sql = "SELECT * FROM CUSTOMERS"


═════════════════════════════════════════════════════════════════════════════
"""

FIX_2_LLM_ROUTING = """
FIX #2: ROUTE COMPLEX PACKAGES TO LLM (INSTEAD OF DETERMINISTIC GENERATION)
═════════════════════════════════════════════════════════════════════════════

PROBLEM:
────────
Current behavior:
  • generate_services_from_semantics() routes ALL packages to deterministic generator
  • Only path is: _generate_deterministic_service_from_unit()
  • The 12 strict rules in PromptTemplate are never used for normal packages
  • Result: Oracle syntax leaks into output

What SHOULD happen:
  • Simple CRUD packages → deterministic (faster)
  • Complex packages (LOOPs, nested IF, multiple tables) → LLM (better)
  • All LLM output enforces the 12 rules from PromptTemplate

EVIDENCE:
─────────
PromptTemplate exists with rules (never used for deterministic):
  "Never use named parameter syntax like p_id = value"
  "Convert all CASE/WHEN to Java switch or ternary"
  "Never call oracle.apex_web_service"
  ... 9 more rules ...

But generate_services_from_semantics only has TWO paths:
  1. _generate_utility_service_from_unit() - no SQL
  2. _generate_deterministic_service_from_unit() - all SQL (calls PromptTemplate!)

Ah wait, it DOES call PromptTemplate! Let me check where...

Oh! Looking at code: PromptTemplate is imported but only used in a few places.
The real path for SQL packages is the deterministic service generator - which
builds Java by concatenating strings, NOT by calling the LLM with PromptTemplate.


IMPLEMENTATION STRATEGY:
────────────────────────

Option A (Simple): Increase complexity threshold
  - Add a method _should_use_llm(unit) that returns True for:
    • Multiple loops (BULK COLLECT, cursor loops)
    • Nested conditionals (3+ levels IF)
    • Multiple tables (JOIN, aggregate queries)
    • Exception handlers with custom errors

  - In generate_services_from_semantics(), call _should_use_llm()
  - If True, call _generate_service_with_llm_and_validation()
  - If False, call deterministic generator

Option B (Better): Use validation feedback
  - The validation loop ALREADY runs on generated code
  - It already detects violations
  - Change: If violations found, regenerate with LLM instead of just logging

We'll implement Option A (cleaner, more control).


IMPLEMENTATION:
────────────────

Location: src/converter/llm_engine.py
Function: generate_services_from_semantics()

NEW METHOD (add to LLMEngine class):
─────────────────────────────────

def _should_use_llm(self, unit: Dict[str, Any]) -> bool:
    """Determine if package is complex enough to require LLM"""
    operations = unit.get('operations_by_table', {})
    semantic = unit.get('semantic_analysis', {})
    raw_plsql = unit.get('raw_plsql', '')
    
    # Count indicators:
    indicators = 0
    
    # Indicator 1: Multiple table operations → JOIN or aggregate
    if len(operations) > 1:
        indicators += 2
    
    # Indicator 2: Multiple transactions (savepoint, rollback)
    transaction = unit.get('transaction', {})
    if transaction.get('has_savepoint') or transaction.get('has_partial_rollback'):
        indicators += 2
    
    # Indicator 3: Exception handling with custom errors
    if unit.get('programmatic_raises'):
        indicators += 1
    
    # Indicator 4: Nested control structures
    nested_ifs = len(re.findall(r'if\s*\(', raw_plsql, re.IGNORECASE))
    nested_loops = len(re.findall(r'for|while|loop', raw_plsql, re.IGNORECASE))
    if nested_ifs >= 3 or nested_loops >= 2:
        indicators += 2
    
    # Indicator 5: Complex aggregations or transformations
    if semantic.get('aggregation', {}).get('columns'):
        indicators += 1
    
    # Threshold: 3+ indicators = use LLM
    return indicators >= 3


MODIFY generate_services_from_semantics():
───────────────────────────────────────────

    for filename in service_order:
        unit = selected_units[filename]
        unit_name = unit.get('name', '')
        service_feedback = feedback_for_service.get(filename, []) or feedback_for_service.get(unit_name, [])
        
        if self._is_utility_unit(unit):
            services[filename] = self._generate_utility_service_from_unit(
                unit=unit,
                all_units=source_units,
            )
        # FIX #2: Route complex packages to LLM
        elif self._should_use_llm(unit):
            services[filename] = await self._generate_service_with_llm(
                unit=unit,
                entities=entities,
                repositories=repositories,
                all_units=source_units,
                validation_feedback=service_feedback,
            )
        else:
            services[filename] = self._generate_deterministic_service_from_unit(
                unit=unit,
                entities=entities,
                repositories=repositories,
                all_units=source_units,
                metadata_provider=metadata_provider,
                validation_feedback=service_feedback,
            )
        
        # Wire corrector (FIX #1)
        services[filename] = ServiceCodeCorrector.correct_plsql_syntax(services[filename])

    return services


NEW METHOD: _generate_service_with_llm():
──────────────────────────────────────────

async def _generate_service_with_llm(
    self,
    unit: Dict[str, Any],
    entities: Dict[str, str],
    repositories: Dict[str, str],
    all_units: List[Dict[str, Any]],
    validation_feedback: Optional[List[str]] = None,
) -> str:
    '''Generate service using LLM with PromptTemplate for complex packages'''
    
    # Build the prompt with PromptTemplate
    prompt = self.template_engine.build_service_prompt_from_unit(
        unit=unit,
        entities=entities,
        repositories=repositories,
        validation_feedback=validation_feedback,
    )
    
    # Call LLM with the strict template
    logger.info(f"Routes to LLM for {unit.get('name', 'unknown')}")
    result = await self.llm_provider.generate_code(prompt)
    
    # Validate output against the 12 rules
    is_valid = await self._validate_java_code(result)
    if not is_valid:
        logger.warning(f"LLM output validation failed, retrying...")
        # Optionally: regenerate with stricter constraints
        # For now: log and continue (corrector will clean it up)
    
    return result


IMPACT:
───────
✓ Complex packages now go through LLM validation (12 rules enforced)
✓ Simple CRUD stays deterministic (fast)
✓ LLM gets better context (knows which packages need special handling)
✓ Validation feedback loop now influences generation decisions

TIME: ~30 minutes to implement + test


═════════════════════════════════════════════════════════════════════════════
"""

FIX_3_REMOVE_UNUSED_LOOP = """
FIX #3: REMOVE USELESS for(rowIndex=0; rowIndex<1) LOOP
═══════════════════════════════════════════════════════

PROBLEM:
────────
Current deterministic generator ALWAYS wraps row logic in:

    for (int rowIndex = 0; rowIndex < 1; rowIndex++) {
        // row logic here
    }

Why? Because pagination code also has this loop (for multiple rows).
But for simple SELECT...INTO or single-record operations, this loop is:
  • Useless (always runs exactly once)
  • Wasteful
  • Confusing to read

EVIDENCE:
─────────
In _generate_deterministic_service_from_unit() around line 2300:

    body_lines.append("for (int rowIndex = 0; rowIndex < 1; rowIndex++) {")
    body_lines.extend(f"    {line}" for line in row_logic)
    body_lines.append("}")

This is ALWAYS added, even when requires_pagination = False


CONDITION FOR ADDITION:
───────────────────────
Loop SHOULD be added when:
  • requires_pagination = True (multiple rows expected)
  • Cursor operations with BULK COLLECT
  
Loop SHOULD NOT be added when:
  • Simple SELECT ... INTO (single record)
  • Single procedure parameters (no loop)
  • Deterministic single-row operation


IMPLEMENTATION:
────────────────

Location: src/converter/llm_engine.py
Function: _generate_deterministic_service_from_unit()

Change around line 2300:

FROM:
────
    body_lines.append("for (int rowIndex = 0; rowIndex < 1; rowIndex++) {")
    body_lines.extend(f"    {line}" for line in row_logic)
    body_lines.append("}")

TO:
───
    # FIX #3: Only add loop if pagination/bulk operations required
    if requires_pagination or needs_bulk_iteration:
        body_lines.append("for (int rowIndex = 0; rowIndex < pageSize; rowIndex++) {")
        body_lines.extend(f"    {line}" for line in row_logic)
        body_lines.append("}")
    else:
        # Simple single-row operation: no loop needed
        body_lines.extend(row_logic)


IMPACT:
───────
✓ Generated code is cleaner for simple operations
✓ Performance slightly better (no useless loop)
✓ Easier to read and understand

SIZE: ~10 lines changed


═════════════════════════════════════════════════════════════════════════════
"""

FIX_4_RESOLVE_LOOKUP_ARGUMENT = """
FIX #4: FIX _resolve_lookup_argument() TO NEVER EMIT NAMED PARAMS
═════════════════════════════════════════════════════════════════

PROBLEM:
────────
Current _resolve_lookup_argument() sometimes returns badly formatted args.

Looking at code around line 2500, this function is supposed to return
the argument to pass to a repository method like:
  
  repository.findById( ← WHERE IT RETURNS )

But it returns things like:
  • "customerId = value" (WRONG - this is PL/SQL named param syntax!)
  • Should return just "customerId" or the variable reference

EVIDENCE:
─────────
Usage around line 2531:
  lookup_args.append(_resolve_lookup_argument(id_column, id_type))
  row_logic.append(f"{driving_repo_var}.{lookup_method}({', '.join(lookup_args)});")

Result is things like:
  repository.findById(customerId = someValue);  ← SYNTAX ERROR!

Should be:
  repository.findById(someValue);  ← CORRECT


IMPLEMENTATION:
────────────────

Location: src/converter/llm_engine.py
Function: _resolve_lookup_argument() (around line 2500)

Current code:
─────────────
def _resolve_lookup_argument(column_name: str, expected_type: str) -> str:
    normalized = normalize_column_name(column_name)
    exact = param_name_map.get(str(column_name).upper())
    if exact:
        return exact  # ← This might be "key = value"!
    normalized_hit = param_name_map.get(normalized.upper())
    if normalized_hit:
        return normalized_hit  # ← This also might be wrongly formatted!
    ...

CHANGE TO:
──────────
def _resolve_lookup_argument(column_name: str, expected_type: str) -> str:
    """FIX #4: Return ONLY the value, never the 'key = value' syntax"""
    normalized = normalize_column_name(column_name)
    exact = param_name_map.get(str(column_name).upper())
    if exact:
        # Strip any 'key = value' syntax, keep only value
        value = exact.split('=')[-1].strip() if '=' in exact else exact
        return value
    
    normalized_hit = param_name_map.get(normalized.upper())
    if normalized_hit:
        # Same treatment
        value = normalized_hit.split('=')[-1].strip() if '=' in normalized_hit else normalized_hit
        return value
    
    if normalized in method_param_names:
        return normalized
    
    for key_name, value_name in param_name_map.items():
        if normalized and normalized.lower() in key_name.lower():
            # Strip '=' syntax here too
            value = value_name.split('=')[-1].strip() if '=' in value_name else value_name
            return value
    
    if join_var and normalized and normalized.lower() == join_var.lower():
        return join_var
    
    return _default_literal(expected_type)


IMPACT:
───────
✓ No more Java named-parameter syntax in method calls
✓ All repository calls have valid Java syntax
✓ Fixes one of the most visible Oracle syntax leaks


═════════════════════════════════════════════════════════════════════════════
"""

FIX_5_EXPAND_VALIDATION = """
FIX #5: EXPAND _validate_java_code() TO CATCH ORACLE FUNCTIONS
═══════════════════════════════════════════════════════════════

PROBLEM:
────────
Current _validate_java_code() (llm_engine.py) only checks:
  • Method signatures
  • Empty methods
  • @PostMapping syntax

It does NOT check for:
  • Oracle SQL functions (INSTR, SUBSTR, NVL, TO_CHAR, TRUNC)
  • APEX calls (apex_web_service.*)
  • PL/SQL naming (p_*, l_*)
  • Single-quoted strings (outside of SQL)
  • Assignment operator :=

So LLM-generated code can still have Oracle syntax that isn't caught.


IMPLEMENTATION:
────────────────

Location: src/converter/llm_engine.py
Function: _validate_java_code() (around line ~800)

Current code (abbreviated):
──────────────────────────
def _validate_java_code(self, code: str) -> bool:
    # Check for empty methods
    if empty_method_pattern:
        return False
    # Check method signature
    if not self._has_valid_method_signature(code):
        return False
    return True

CHANGE TO:
──────────
def _validate_java_code(self, code: str) -> bool:
    """FIX #5: Comprehensive validation against 12 rules"""
    
    # 1. Check for empty methods
    if re.search(r'public\s+\w+\s+\w+\s*\([^)]*\)\s*\{\s*\}', code):
        logger.warning("Validation: Empty method found")
        return False
    
    # 2. Check method signature exists
    if not self._has_valid_method_signature(code):
        logger.warning("Validation: Invalid method signature")
        return False
    
    # 3. FIX #5: Check for Oracle SQL functions (CASE-INSENSITIVE)
    oracle_functions = ['instr', 'substr', 'nvl', 'to_char', 'trunc', 'round']
    for func in oracle_functions:
        if re.search(rf'\b{func}\s*\(', code, re.IGNORECASE):
            logger.warning(f"Validation: Oracle function '{func}' found")
            return False
    
    # 4. FIX #5: Check for APEX calls
    if 'apex_web_service' in code.lower():
        logger.warning("Validation: apex_web_service found (use RestTemplate)")
        return False
    
    # 5. FIX #5: Check for PL/SQL naming patterns (p_*, l_*)
    if re.search(r'\b[pl]_\w+\b', code):
        logger.warning("Validation: PL/SQL naming prefix found (p_*, l_*)")
        return False
    
    # 6. FIX #5: Check for := operator
    if ':=' in code:
        logger.warning("Validation: PL/SQL assignment :=  found")
        return False
    
    # 7. FIX #5: Check for named parameter syntax
    if re.search(r'(\w+)\s*=\s*(\w+)\s*[,\)]', code):
        # This is tricky - could be part of an annotation or actual assignment
        # Only flag if it looks like a method call
        if re.search(r'\.(\w+)\s*\s*(\w+\s*=\s*\w+)', code):
            logger.warning("Validation: Named parameter syntax found")
            return False
    
    # 8. FIX #5: Check for invalid JPA patterns
    if re.search(r'\.findOne\s*\(\s*null\s*\)', code):
        logger.warning("Validation: findOne(null) found - use findById")
        return False
    
    return True


INTEGRATION:
─────────────
Call this enhanced validation in:
  1. After LLM generates code: _generate_service_with_llm()
  2. After deterministic generation (before corrector): generate_services_from_semantics()

Example:
    services[filename] = self._generate_deterministic_service_from_unit(...)
    
    # Validate before correction
    if not self._validate_java_code(services[filename]):
        logger.warning(f"Generated code has validation issues: {filename}")
        # Could regenerate with LLM, but for now just log
    
    # Apply Fix #1 corrector as safety net
    services[filename] = ServiceCodeCorrector.correct_plsql_syntax(services[filename])


IMPACT:
───────
✓ Acts as early warning system for bad code generation
✓ Catches Oracle syntax BEFORE it gets written to disk
✓ Can trigger regeneration if desired (future enhancement)
✓ Provides detailed logging for debugging


═════════════════════════════════════════════════════════════════════════════
"""

FIX_6_INNER_CLASS_PLACEMENT = """
FIX #6: MOVE INNER BUSINESSEXCEPTION CLASS OUTSIDE METHOD BODY
═══════════════════════════════════════════════════════════════

PROBLEM:
────────
Generated service code has this structure:

    public class MyService {
        public void doSomething() {
            // method logic
            
            // WRONG: Inner class inside method!
            class BusinessException extends Exception {
                public String getErrorCode() { ... }
            }
        }  ← Closing brace of method
    }

This is syntactically invalid Java. Inner classes must be at CLASS level,
not inside methods.

RESULT: Generated Java does not compile.


IMPLEMENTATION:
────────────────

Location: src/converter/llm_engine.py
Function: _generate_deterministic_service_from_unit()

Current code (around line 2800, end of service):
────────────────────────────────────────────────
    # Build service class string
    service_lines = [
        "import org.springframework.stereotype.Service;",
        f"public class {service_name} {{",
        *method_lines,
        "}"
    ]
    
    # Build exception class (WRONG PLACEMENT)
    if has_business_exception:
        exception_lines = [
            f"class {business_exception_name} extends Exception {{",
            ...
            "}",
        ]
        # Currently inserted at END of method lines!

CHANGE TO:
──────────
    # Separate method and exception class
    method_body_lines = [...]  # Method lines
    
    # Exception class lines
    exception_lines = []
    if has_business_exception:
        exception_lines = [
            "",
            f"public static class {business_exception_name} extends Exception {{",
            '    private String errorCode;',
            f'    public {business_exception_name}(String message, String code) {{',
            '        super(message);',
            '        this.errorCode = code;',
            '    }',
            '    public String getErrorCode() {',
            '        return errorCode;',
            '    }',
            "}",
        ]
    
    # Build service class string with CORRECT structure:
    service_lines = [
        "import org.springframework.stereotype.Service;",
        "import java.util.logging.Logger;",
        f"public class {service_name} {{",
        *method_body_lines,  # Methods (correctly closed)
        *exception_lines,      # Exception class (at CLASS level, not inside method)
        "}"
    ]


IMPACT:
───────
✓ Generated Java now compiles successfully
✓ ServiceBusinessException is properly accessible
✓ Eliminates syntactical errors preventing compilation


═════════════════════════════════════════════════════════════════════════════
"""

# ============================================================================
# IMPLEMENTATION ROADMAP & EXECUTION PLAN
# ============================================================================

IMPLEMENTATION_ROADMAP = """
IMPLEMENTATION ROADMAP - PRIORITY & EXECUTION ORDER
════════════════════════════════════════════════════

PHASE 1: QUICK WINS (15 minutes)
────────────────────────────────
These are low-risk, high-impact changes with minimal side effects.

✓ FIX #1: Wire corrector into pipeline (1 line)
  - File: llm_engine.py
  - Add one import + one call
  - Time: 5 minutes
  - Risk: Very low (corrector already tested)

✓ FIX #6: Move inner class outside method (string manipulation)
  - File: llm_engine.py
  - Rearrange generated code structure
  - Time: 5 minutes
  - Risk: Very low (just reorganizing strings)

✓ FIX #3: Remove useless for-loop (conditional logic)
  - File: llm_engine.py
  - Add condition: don't wrap if no pagination
  - Time: 5 minutes
  - Risk: Low (affects loop structure only)


PHASE 2: CORE FIXES (45 minutes)
────────────────────────────────
These are more involved changes to generation logic.

✓ FIX #4: Fix _resolve_lookup_argument() (value cleanup)
  - File: llm_engine.py
  - Function: _resolve_lookup_argument()
  - Time: 15 minutes
  - Risk: Medium (touches parameter resolution)
  - Testing: Verify no named-param syntax in output

✓ FIX #5: Expand _validate_java_code() (regex patterns)
  - File: llm_engine.py
  - Function: _validate_java_code()
  - Time: 20 minutes
  - Risk: Medium (adds more validation)
  - Testing: Verify catches all 12 rule violations

✓ FIX #2: Route complex packages to LLM (conditional + method)
  - File: llm_engine.py
  - Functions: _should_use_llm() + _generate_service_with_llm()
  - Time: 30 minutes
  - Risk: Medium-High (changes routing logic)
  - Testing: Verify both deterministic and LLM paths work


PHASE 3: TESTING & VALIDATION (60+ minutes)
───────────────────────────────────────────
Comprehensive testing of all changes together.

✓ Unit tests for each fix
  - Test _resolve_lookup_argument() with edge cases
  - Test _validate_java_code() catches all patterns
  - Test corrector removes all PL/SQL syntax

✓ Integration tests
  - Generate project from plsql_sample_repo
  - Verify generated Java compiles: mvn clean compile
  - Check for any remaining Oracle syntax

✓ End-to-end tests
  - Generate full Spring Boot project
  - Run tests: mvn clean test
  - Manual code review of samples

✓ Regression tests
  - Ensure existing working generation still works
  - No new errors introduced


TOTAL TIME ESTIMATE: 2-3 hours


═════════════════════════════════════════════════════════════════════════════
TESTING STRATEGY
════════════════════════════════════════════════════════════════════════════

Test Package Setup:
  • Use procedures from plsql_sample_repo for real-world examples
  • Create both simple and complex test cases


Test Cases for FIX #1 (Corrector):
──────────────────────────────────
Input:
  p_customer_id := getCustomerId()
  String status = SUBSTR(name, 1, 10)
  String sql = 'SELECT * FROM CUSTOMERS'

Expected Output (after corrector):
  pCustomerId = getCustomerId()
  String status = name.substring(0, Math.min(10, name.length()))
  String sql = "SELECT * FROM CUSTOMERS"

Verify: Corrector removes all patterns


Test Cases for FIX #2 (LLM Routing):
────────────────────────────────────
Simple package (no loop, no joins):
  → Should NOT use LLM (stays deterministic)
  
Complex package (multiple joins, transactions):
  → Should use LLM (calls _generate_service_with_llm())

Verify: Both paths produce valid Java


Test Cases for FIX #4 (_resolve_lookup_argument):
─────────────────────────────────────────────────
Before:
  repository.findById(customerId = someValue)
  
After:
  repository.findById(someValue)

Verify: No '=' in method arguments


Test Cases for FIX #5 (Validation):
───────────────────────────────────
Test code with each Oracle function:
  INSTR(str, 'x') → Should fail validation
  SUBSTR(str, 1, 10) → Should fail validation
  NVL(val, 0) → Should fail validation
  apex_web_service.call() → Should fail validation
  p_customerId := value → Should fail validation

Verify: Validation catches all patterns


═════════════════════════════════════════════════════════════════════════════
"""

# ============================================================================
# SUMMARY & NEXT STEPS
# ============================================================================

SUMMARY = """
COMPREHENSIVE FIX SUMMARY
═════════════════════════

WHAT'S BROKEN:
──────────────
1. Generator produces Oracle PL/SQL syntax in Java code
2. All packages route to deterministic Python templates (LLM bypassed)
3. Invalid JPA patterns (findOne(null), named params)
4. Corrector exists but isn't called post-generation
5. Inner classes placed inside methods (syntax error)
6. Useless for-loops wrap all logic

ROOT CAUSE:
───────────
generate_services_from_semantics() only calls _generate_deterministic_service_from_unit()
This deterministic generator builds Java by string concatenation with NO AI validation.

THE 6 FIXES:
────────────
1. Wire corrector into pipeline (1 line)
2. Route complex packages to LLM (conditional + method)
3. Remove useless for-loops (conditional)
4. Fix _resolve_lookup_argument() to strip named params
5. Expand validation to catch Oracle syntax
6. Move inner class outside method (string rearrange)

EXPECTED OUTCOME:
─────────────────
✓ All generated Java compiles without syntax errors
✓ No Oracle syntax mixed into Java
✓ Proper JPA patterns throughout
✓ All 12 PromptTemplate rules enforced
✓ Production-ready code from generator

IMPLEMENTATION:
────────────────
Time: 2-3 hours total
Risk: Low-Medium (each fix is isolated)
Priority: CRITICAL (required for working generator)

NEXT STEPS:
───────────
1. Start with PHASE 1 (quick wins): 15 min
2. Proceed to PHASE 2 (core fixes): 45 min
3. Execute PHASE 3 (testing): 60+ min
4. Review + deploy
"""

if __name__ == "__main__":
    print(EXECUTIVE_SUMMARY)
    print(FIX_1_CORRECTOR_INTEGRATION)
    print(FIX_2_LLM_ROUTING)
    print(FIX_3_REMOVE_UNUSED_LOOP)
    print(FIX_4_RESOLVE_LOOKUP_ARGUMENT)
    print(FIX_5_EXPAND_VALIDATION)
    print(FIX_6_INNER_CLASS_PLACEMENT)
    print(IMPLEMENTATION_ROADMAP)
    print(SUMMARY)
